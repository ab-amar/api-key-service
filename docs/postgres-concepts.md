# PostgreSQL Concepts

PostgreSQL gives the project:

- durable storage
- constraints
- indexes
- transactional behavior

Important concepts for this project:

- primary keys
- unique constraints
- timestamp columns
- query performance for hash lookups
