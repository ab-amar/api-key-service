# Observability

The three common observability pillars are:

- logs
- metrics
- traces

For this project:

- logs help debug authentication and rate limiting decisions
- metrics help track request volume, auth failures, and rate-limit rejections
- traces are useful conceptually, but can remain out of scope for a smaller implementation
