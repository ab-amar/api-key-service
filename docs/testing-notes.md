# Testing Notes

Topics that support the implementation tasks:

- table-driven tests
- `httptest`
- `testify`

Table-driven tests help when:

- many inputs share the same test shape

`httptest` helps when:

- simulating HTTP requests and responses in memory

`testify` helps when:

- assertions become repetitive and harder to read with plain `testing`
